import PokemonContainer from "../PokemonContainer";

export default function PokemonTable() {
  return (
    <section>
      <ul>
        <PokemonContainer />
        <PokemonContainer />
        <PokemonContainer />
        <PokemonContainer />
      </ul>
    </section>
  );
}
